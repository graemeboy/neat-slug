var textFilter = require('./lib/textFilter');


/**
 * A array of strings ordered by their popularity as hashtags
 * on Twitter.
 */
var hashtagList;



function getHashtagPopularity(str, callback) {

}

/**
 * Order the list by the popularity of its items as hashtags.
 * 
 * @param  list, a list of words
 * @return orderedList
 */
function orderListByPopularity(list) {

}

// For testing:
exports.textFilter = textFilter;